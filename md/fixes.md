shortening some tailwind classes
subscription for the /ultra and pro plan
overflow with due before dateinput in /tasks
tasks that spans from 10 PM - 02 AM in todays page
height in the todays plan

disable tab navigation isnide /notes page because of writing
overflow of export as TXT - note

In TaskCardSmall.tsx, add an info next to the task duration that tells the user the starting and endtime of the task
If no startTime but endTime is specified: To 22:00
if no endTime but startTime: From 16:00
if both: 16:00 - 22:00

Some of the exercise I put inside @exerciseLibrary are based on a duration (holds). For example
