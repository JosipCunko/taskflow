# Caching problem when I get the log: 6 repeating tasks updated for user: (...), none of them actually got updated, maybe in the firestore, but in the UI, everything is stale until I click refresh inside /tasks loged in with another machine and added a task, went back to the old phone, refresh the page, that task online showed up when clicked refresh in /tasks

Also, when I click refresh on the /tasks page, I seems that nothing was refreshed when I go to the dashboard. What is wrong with our caching and revalidating system.

# Repeating tasks inside /calendar I want to show repeating tasks inside calendar page its easy to setup daysofweek tasks inside /calendar, but the problem is how we are going to display interval tasks and timesperweek tasks

# Workout templates

when I create a workout from a template, it always says that it is still in progress and I can always edit it or finish it, that doesnt make sense

# exerciseslibrary

https://api-ninjas.com/api/exercises#exercises-endpoint
